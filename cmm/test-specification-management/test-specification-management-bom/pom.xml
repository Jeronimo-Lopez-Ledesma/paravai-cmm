<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- ========================================================= -->
    <!--                         PARENT                            -->
    <!-- ========================================================= -->
    <parent>
        <groupId>com.paravai.regulations</groupId>
        <artifactId>test-specification-management</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!-- ========================================================= -->
    <!--                     BOM COORDINATES                       -->
    <!-- ========================================================= -->
    <artifactId>test-specification-management-bom</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>Test Specification Management :: BOM</name>
    <description>
        Domain-specific dependency versions for Test Specification Management services.
        Contains internal module versions only. External dependencies come from service-bom.
    </description>

    <!-- ========================================================= -->
    <!--                 DOMAIN-SPECIFIC DEPENDENCIES              -->
    <!-- ========================================================= -->
    <properties>
        <service.foundation.version>2.1.0</service.foundation.version>
    </properties>
    <dependencyManagement>
        <dependencies>

            <!-- Shared utilities inside this CMM -->
            <dependency>
                <groupId>com.paravai.regulations</groupId>
                <artifactId>test-specification-management-common</artifactId>
                <version>1.0.0</version>
            </dependency>

            <!-- Standards aggregate -->
            <dependency>
                <groupId>com.paravai.regulations</groupId>
                <artifactId>standards</artifactId>
                <version>1.0.0</version>
            </dependency>

            <!-- Standard Relationships aggregate-->
            <dependency>
                <groupId>com.paravai.regulations</groupId>
                <artifactId>standard-relationships</artifactId>
                <version>1.0.0</version>
            </dependency>

            <!-- Test Specifications aggregate -->
            <dependency>
                <groupId>com.paravai.regulations</groupId>
                <artifactId>tests-specifications</artifactId>
                <version>1.0.0</version>
            </dependency>

            <!-- Test Cases bounded context -->
            <dependency>
                <groupId>com.paravai.regulations</groupId>
                <artifactId>tests-cases</artifactId>
                <version>1.0.0</version>
            </dependency>

            <!-- Orchestrator (single orchestrator for the whole subdomain) -->
            <dependency>
                <groupId>com.paravai.regulations</groupId>
                <artifactId>composition</artifactId>
                <version>1.0.0</version>
            </dependency>

            <!-- Read Model (single readmodel for the whole subdomain) -->
            <dependency>
                <groupId>com.paravai.regulations</groupId>
                <artifactId>readmodel</artifactId>
                <version>1.0.0</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

</project>
